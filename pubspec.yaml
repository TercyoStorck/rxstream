name: rxstream
description: A lightweight StreamController abstraction that mimics BehaviorSubject.
version: 1.1.0
homepage: https://github.com/TercyoStorck/

environment:
  sdk: ">=2.15.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
